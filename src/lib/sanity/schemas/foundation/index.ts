import foundationLocalization from "./foundationLocalization";
import foundationBehavior from "./foundationBehavior";
import foundationTheme from "./foundationTheme";
import foundationDesign from "./foundationDesign";

export const foundationSchemas = [
  foundationLocalization,
  foundationBehavior,
  foundationTheme,
  foundationDesign,
];

export default foundationSchemas;
